#include <stdio.h>
/**
 * main - main fctn
 * Return: 0(Succes)
 */
int main(void)
{
int alpha;
for (char(alpha) = 48; char(alpha) <= 57; char(alpha)++)
{
putchar(alpha);
}
putchar('\n');
return (0);
}

