#include <stdio.h>
/**
 * main - main fctn
 * Return: 0(Succes)
 */
int main(void)
{
char alpha;
for (alpha = 48; alpha <= 57; alpha++)
{
putchar(alpha);
}
for (alpha = 97; alpha <= 102; alpha++)
{
putchar(alpha);
}
putchar('\n');
return (0);
}

